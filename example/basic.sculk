fn add(a: int, b: int, c: int) -> int {
    return a + b + c;
}

fn determinant(a: int, b: int, c: int) -> int {
    return b * b - 4 * a * c;
}

fn pow(a: int, b: int) -> int {
    let c = a;

    for let i = 1; i < b; i += 1 {
        c *= a;
    }

    return c;
}